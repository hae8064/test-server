# 이 파일을 .env로 복사한 뒤 실제 값으로 수정하세요: cp .env.example .env

# Server
PORT=3000

# Database (PostgreSQL)
# docker-compose로 postgres 실행 시 아래 값을 사용합니다.
DB_HOST=localhost
DB_PORT=5436
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=biocom

# 개발 단계: synchronize=true (기본값). 프로덕션에서 false로: DB_SYNCHRONIZE=false

# JWT (admin 로그인용)
JWT_SECRET=your_jwt_secret_change_in_production
JWT_EXPIRES_IN=7d

# 로컬 실행: pnpm run dev (postgres + nest 함께), 또는 pnpm run db:start 후 pnpm run start:dev
